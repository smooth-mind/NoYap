# Controlled changes

- `proposed/`: impact analysis exists, but implementation is not permitted.
- `approved/`: required approval exists and implementation conditions are explicit.
- `completed/`: implementation and documentation are complete.

Moving a file between folders should preserve its ID and decision history.
